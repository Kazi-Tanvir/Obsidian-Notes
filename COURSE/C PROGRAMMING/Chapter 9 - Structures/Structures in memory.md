# Structures in memory
Structures are stored in contiguous memory locations. For the structure ‘e1’ of type `struct employee`, memory layout is contiguous.

In an array of structures, these employee instances are stored adjacent to each other.
