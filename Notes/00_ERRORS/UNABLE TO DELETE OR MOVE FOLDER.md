---
date: 2026-07-01
tags:
  - file-edit
---
`Error Message`
```cmd
file in use
The action can't be completed because the folder or a file in it is open in
another program
```

`Fix `
- go to `WIN` `+` `R` -> `resmon `-> `cpu` ->`Search Handles` -> `end process` for the particular folder